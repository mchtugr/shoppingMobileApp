import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  closeButton: {
    position: 'absolute',
    left: 20,
  },
  deleteButton: {
    position: 'absolute',
    right: 20,
    left: 'auto',
  },
})

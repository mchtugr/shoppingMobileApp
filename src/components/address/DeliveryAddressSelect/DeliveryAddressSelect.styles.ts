import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    display: 'flex',
    alignItems: 'center',
    flex: 1,
  },
  inputContainer: {
    backgroundColor: '#dedede',
    alignItems: 'center',
    padding: 5,
    width: 300,
    marginTop: 10,
  },
})
